spring:
  datasource:
    url: jdbc:mysql://localhost:3306/eurekapp
    username: eurekapp
    password: eurekapp
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

application:
  s3:
    bucket:
      name: 'eurekapp-temp-local'
  openai:
    url: 'https://api.openai.com/v1/'
    api-key: ${OPENAI_SECRET_KEY}           # requerido: setear en .env.local
  jwt:
    sign-key: ${JWT_SIGN_KEY}               # requerido: setear en .env.local
  mailtrap:
    host: 'live.smtp.mailtrap.io'
    user: 'api'
    password: ${MAILTRAP_KEY}               # requerido: setear en .env.local
    port: '587'
  weaviate:
    url: 'localhost:8081'
    schema: 'http'

search:
  max-radius: 50000
